import 'package:flutter/material.dart';

class DatabaseConnection {
  // setDatabase() async{
  //   var directory = await getApplicationDocumentsDirectory();
  //   var path = join(directory.path, 'db_recetas_sqfite');
  //   var database = await openDatabase(path,version: 1, onCreate: _onCreateDatabase);
  //
  //   return database;
  // }
  //
  // _onCreateDatabase(Database database, int version ) async{
  //   await database.execute(
  //       "CREATE TABLE categories (id INTEGER PRIMARY KEY, name TEXT, description TEXT)"
  //   );
  //
  //   //crear tabla de todos
  //   await database.execute(
  //       "CREATE TABLE todos  (id INTEGER PRIMARY KEY, title TEXT, description TEXT, category TEXT, date TEXT, isFinished INTEGER)"
  //   );
  // }
}